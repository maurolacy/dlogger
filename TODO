Database
  - Simplify tables structure
    - Remove duration / interval fields

Complete install instructions
 - Test steps / re-install
 - Create missing install scripts (sensors)

Fix corrupted data / zero bytes on reboot / crash
  mean_temperature.txt (append mode)

Use start-stop-daemon(done for humidity/pressure)
  Fix logging (syslog?)

Plot infrastructure
  - Add selected button highlight

-temperature

- humidity
  Fix / use kernel driver (dht11) (wip)

- encoder:
  rotary-encoder kernel driver
    modify params (gpio_a, gpio_b)

- duster
  client/server architecture
  GUI client
