Use start-stop-daemon(done for humidity)
    "daemons" logging

Fix publish/plot infrastructure

- encoder:
    rotary-encoder kernel driver
        modify params (gpio_a, gpio_b)

- duster
	client/server architecture
	GUI client
