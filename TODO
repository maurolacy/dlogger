Use start-stop-daemon(done for humidity/pressure)
  Fix logging (syslog?)

Plot infrastructure
  - dygraphs (done)
  - Add selections for: all data, yearly, monthly, weekly, daily

-temperature
  - average of both sensors (pressure and humidity)
  - plot custom bars

- humidity
  fix / use kernel driver (dht11)

- encoder:
  rotary-encoder kernel driver
    modify params (gpio_a, gpio_b)

- duster
  client/server architecture
  GUI client
